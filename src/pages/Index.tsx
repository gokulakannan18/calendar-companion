import { WallCalendar } from "@/components/calendar/WallCalendar";

export default function Index() {
  return (
    <main className="flex min-h-screen items-start justify-center bg-background py-8 sm:py-12">
      <WallCalendar />
    </main>
  );
}
